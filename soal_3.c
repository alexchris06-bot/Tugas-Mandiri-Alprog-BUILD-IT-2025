#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
int panjangString;
char kalimat[1000];
int jumlahKata = 0;
char kata[300][71];
int panjangKata[300];

    printf("Masukkan kalimat anda: ");
    scanf("%s", &kalimat);
    
    for(int i = 0; ; i++){
        char c = line[i];
        if(c = ' ' && c = '\0'){
            if(c < 71)
        }
    }
    else{
        if(c > 0){
            c = '\0''
                c = 0;
        }
    }
    printf("Hasil kalimat: ");
    for(int i = 0; i < kata; i++){
        int kata = 0;
        for (int j = 0; j < 1; j++){
            if(caseInsensitive){
                if(strlen(kata[i]) == strlen(kata[j]); k++){
                    sama = 1;
                    break;
                }
            }
        }
    }
    if (!sama){
        printf("%s", &kata);
    }
    
    printf("\n");
    return 0;
}
